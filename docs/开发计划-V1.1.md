# 开发计划 V1.1 — 规格差异收敛与功能增量

> 版本：v1.0 | 日期：2026-08-08
> 前置文档：《UI动效优化任务清单》（v1.1 完结版）、《spec-vs-impl 差异清单》、《交互规格说明书（带实现状态标注）》

---

## 一、当前项目状态分析

### 已完成（截至 2026-08-08 实测）

| 类别 | 内容 | 验证证据 |
|---|---|---|
| 动效任务清单 16 项 | T1.1–T1.4 缓动升级、T2.1–T2.5 设置界面动效、T3.1–T3.4 气泡视觉升级、T4.1–T4.2 规范与技能沉淀 | 全部落地，见《UI动效优化任务清单》v1.1 完结标注 |
| Rust 后端 | 状态机（含 FadingIn 入场）、easing 模块、软阴影/渐变渲染、主题配色预设、连击乘数动画、持久化恢复 | `cargo test` 46/46 通过 |
| 前端设置界面 | Tab 转场、模态动画、Toast 转场、滑块实时预览、a11y 全绿 | `svelte-check` 0 errors / 0 warnings；前端单测 6/6 |
| 核心功能 | 全局按键/鼠标监听、三种教学模式预设、托盘、全局热键、黑名单过滤（前台窗口检测已生效） | `lib.rs` `is_foreground_blacklisted` |

### 未完成（规格缺口）

依据《spec-vs-impl 差异清单》与《交互规格说明书》：

| # | 缺口 | 规格出处 | 现状 |
|---|---|---|---|
| G1 | HIDDEN 态 + 托盘图标闪烁 | §1.1、§6.1、§8.1 | ❌ 未实现 |
| G2 | DWM 全屏独占检测 | §8.1 | 🔀 以进程黑名单替代（已生效） |
| G3 | 淡出时长 280ms vs 规格 300ms 的决策记录 | §1.4 | ⚠️ 文档未补记 |
| G4 | `quadrant` 字段去留决策 | 差异 #4 | ℹ️ 待决策 |
| G5 | 首次启动向导（3 步） | §5 | ❌ 未实现 |
| G6 | 高 DPI `WM_DPICHANGED`、多显示器热插拔 | §8.2、§8.3 | ❌ 未实现 |
| G7 | 鼠标涟漪效果（V1.1 可选） | §2.4 | ⏸️ 规格本身标注可选、默认关闭 |

---

## 二、任务优先级排序与实施步骤

### 阶段 0：收尾与基线固化 ✅ 已完成（2026-08-08）

- [x] 0.1 更新 `VERIFICATION.md`（26/26 → 46/46，补记 easing/渲染器/持久化测试）
- [x] 0.2 前端检查清零：svelte-check 0 errors / 0 warnings（含 17 个 a11y 警告修复：label 关联、`role="group"` + `aria-labelledby`、模态 backdrop 的 target 判断模式）
- [x] 0.3 全量验证：`cargo test` 46/46、前端单测 6/6、svelte-check 全绿
- [x] 0.4 将《UI动效优化任务清单》标记为 v1.1 完结版
- [x] 0.5 提交当前工作区基线（提交 ea989a4：23 个文件，含 scratch 临时文件清理与 .gitignore 补充）

### 阶段 1：规格差异收敛（约 2 人日）

| # | 任务 | 建议决策 | 理由 |
|---|---|---|---|
| 1.1 | G2 全屏检测方案定案 | **修规格**：明确"黑名单为主、DWM 为 V1.1 后期可选增强" | 黑名单已在 `is_foreground_blacklisted` 生效，覆盖主要场景；DWM 检测投入产出比低 |
| 1.2 | G4 `quadrant` 字段决策 | **保留**，在规格 §1.5 补充说明 | 字段已被持久化解析与设置界面（象限选择器）引用，删除有迁移成本 |
| 1.3 | G3 淡出时长补记 | 在规格 §1.4 补记"280ms 为 60fps 帧率对齐的工程优化" | 纯文档动作，消除文档漂移 |

### 阶段 2：V1.1 核心增量 — 首次启动向导（约 3–4 人日）

规格 §5 是当前最大块的未实现项，且与三种教学模式预设强耦合：

| # | 任务 | 说明 |
|---|---|---|
| 2.1 | 3 步向导：场景选择 → 权限提示 → 确认预览 | 场景卡片直接映射 classroom/recording/streaming 三套预设，把"开箱即教学"做实 |
| 2.2 | 向导动效复用既有基线 | fade/scale 转场 + `prefers-reduced-motion`，遵循《动效设计规范》 |
| 2.3 | "重新运行向导"入口 | 设置 → 关于，规格 §5.1 明确要求 |
| 2.4 | 向导完成状态持久化 | 走 tauri-plugin-store，防御式解析，与现有 `apply_persisted_settings` 模式一致 |

### 阶段 3：明确延后项（V1.1 后期或 V1.2）

| 任务 | 延后理由 |
|---|---|
| G1 HIDDEN 态 + 托盘闪烁 | 差异清单已判定低优先级（全屏时可手动暂停），依赖托盘 API 深度集成 |
| G2 DWM 全屏检测 | 黑名单方案已生效，等真实用户反馈漏网场景再投入 |
| G7 鼠标涟漪效果 | 规格标注"V1.1 可选、默认关闭"，教学投影场景反而建议关闭 |
| G6 高 DPI / 多显示器热插拔 | 边界场景，建议收集真实反馈后排期 |
| 性能专项（单帧 < 8ms 打点） | 软阴影/渐变已上线，建议随阶段 2 顺手做一次基准测量，不单独立项 |

---

## 三、时间安排建议

| 阶段 | 工作量 | 建议窗口 |
|---|---|---|
| 阶段 0（收尾） | ✅ 已全部完成 | — |
| 阶段 1（差异收敛） | 约 2 人日 | 第 1 周 |
| 阶段 2（启动向导） | 约 3–4 人日 | 第 2 周 |
| 阶段 3（延后项） | 视反馈 | V1.1 后期 / V1.2 |

---

## 四、验收门槛（每阶段通用）

- `npm run test:all`（svelte-check + cargo test）全绿
- svelte-check 保持 0 errors / 0 warnings
- 动效改动须符合《动效设计规范》数值（入场 120ms / 淡出 280ms / UI 转场 150–200ms / easeOutCubic）
- 规格数值变更须同步更新《交互规格说明书》实现状态标注

---

**文档维护者**: Qoder
**最后更新**: 2026-08-08
**下次审查建议**: 阶段 1 完成后
