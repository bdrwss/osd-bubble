# 按键 OSD 可视化工具 · 功能概念定义文档

> 版本：V1.0-draft ｜ 日期：2026-07-29 ｜ 所属产品：电脑教学小工具集合

---

## 1. 功能定位

**一句话定义：** 在演示者操作电脑时，将当前按键/点击动作以轻量气泡形式实时显示在光标附近，让观看者"看见"每一步操作。

### 目标用户画像

| 维度 | 描述 |
|------|------|
| 核心角色 | 计算机课程教师、企业内训讲师、录屏教程作者 |
| 技术水平 | 熟悉基本电脑操作，但不一定有开发背景；偏好"开箱即用" |
| 使用动机 | 投屏/录屏时观众无法看到键盘操作，需要一种零侵入的可视化补充 |
| 使用环境 | Windows 教室机房（主力）、macOS 个人电脑、偶尔 Linux 演示 |

### 典型使用场景

1. **课堂投屏演示**——教师在全班投影上操作 Excel / 编程 IDE，学生需要同时看到"按了什么键"和"屏幕上发生了什么"。
2. **企业内训录屏**——培训师录制操作教程视频，后期无需逐帧配音解释"这里我按了 Ctrl+Shift+T"。
3. **远程直播教学**——主播共享屏幕时，观众端分辨率有限，按键气泡比口头描述更直观、可回溯。
4. **新手引导/考试演示**——在机房环境中，教师用该工具逐步演示操作流程，学生跟练时能对照气泡确认自己的操作是否一致。

---

## 2. 命名与术语

### 概念解释

"按键 OSD"借用了显示器/电视领域 OSD（On-Screen Display，屏幕叠加显示）的术语，指在不改变目标应用画面的前提下，于屏幕最顶层叠加一层半透明图形，实时呈现输入事件。其核心特征是：

- **非侵入**：不修改任何应用窗口内容，不注入进程。
- **即时性**：从按键到气泡出现延迟 < 50 ms（体感无延迟）。
- **瞬态**：气泡自动消失，不持久占据屏幕空间。

### 中文功能名候选

| 候选名 | 理由 | 顾虑 |
|--------|------|------|
| **按键气泡**（推荐） | 直觉、口语化，"气泡"暗示轻量与瞬态；与微信消息气泡心智一致，降低认知成本 | 略偏可爱，正式场合可能需要副标题补充 |
| 按键提示 | 通用、无歧义，适合写入菜单/设置项 | 与输入法"候选提示"易混淆 |
| 操作浮标 | 强调"浮于屏幕之上"，技术感强 | "浮标"在中文里更常见于航海/钓鱼语境，辨识度低 |

**推荐：产品对外名称用「按键气泡」，技术文档/设置项中保留「按键 OSD」作为术语。**

---

## 3. 可视化对象规则

**结论：V1.0 覆盖键盘单键、组合键、鼠标三键单击、滚轮；双击与长按作为增强项在 V1.1 支持。**

### 3.1 键盘

| 输入类型 | 是否显示 | 呈现方式 |
|----------|----------|----------|
| 单字符键（A、1、;） | ✅ | 显示字符本身，大写字母形态（如 `A`） |
| 功能键 / 控制键 | ✅ | 显示符号化名称：`Esc` `Tab` `Enter` `Backspace` `Space`（显示为 `␣`）`↑↓←→` |
| 组合键（Ctrl+C） | ✅ | 用 `+` 连接，修饰键在前：`Ctrl + C`；macOS 显示 `⌘ + C` |
| 多键同时按住（≥3） | ✅ | 全部列出，如 `Ctrl + Shift + S` |
| 纯修饰键按下（仅按住 Ctrl 未释放） | ❌ V1.0 不显示 | 避免按住不放时气泡常驻；V1.1 可配置"修饰键预显" |
| 输入法候选选词 | ❌ | 属于输入法内部事件，跨平台一致性差，明确排除 |

### 3.2 鼠标

| 输入类型 | 是否显示 | 呈现方式 |
|----------|----------|----------|
| 左键单击 | ✅ | 光标旁出现涟漪动画 + 标签 `L` |
| 右键单击 | ✅ | 涟漪 + 标签 `R` |
| 中键 / 侧键 | ✅ | 涟漪 + 标签 `M` / `X1` / `X2` |
| 双击 | V1.1 | 涟漪 + `L×2` |
| 长按（>500 ms） | V1.1 | 涟漪持续 + 计时环 |
| 滚轮上/下 | ✅ | 小型箭头 `▲` / `▼` + 滚动格数（如 `▲×3`） |
| 水平滚轮 | ✅ | `◀` / `▶` |
| 拖拽过程 | ❌ | 仅显示按下与释放两个端点，避免高频刷新 |

### 3.3 呈现符号规范

- 键盘气泡：圆角矩形，深色半透明底（rgba(0,0,0,0.75)），白色文字，等宽字体。
- 鼠标气泡：以点击坐标为圆心的涟漪圆环（直径 28→48 px 扩散），旁附按键标签。
- 组合键中修饰键使用统一缩写：`Ctrl` `Shift` `Alt` `Win` / `⌘` `⌥` `⇧` `⌃`。

---

## 4. OSD 呈现规则

**结论：气泡跟随光标、右下方偏移、自动避让屏幕边缘；单条显示 1.2 s 后淡出；连续输入采用"替换+延长"策略而非无限堆叠。**

### 4.1 位置

- 默认锚点：光标位置右下方偏移 (16, 20) px（逻辑像素）。
- 避让规则：当气泡预估区域超出当前显示器工作区时，按"右→左、下→上"顺序翻转锚点，保证完整可见。
- 多显示器：气泡始终出现在触发输入事件时鼠标所在的那块屏幕上。

### 4.2 尺寸层级

| 层级 | 内容 | 字号（逻辑 px） | 气泡最大宽度 |
|------|------|-----------------|--------------|
| L1 单键 | `A` / `Enter` | 20 | 120 px |
| L2 组合键 | `Ctrl + Shift + S` | 18 | 260 px |
| L3 鼠标标签 | `L` / `▲×3` | 14 | 64 px |

气泡高度自适应，内边距 8×12 px。

### 4.3 时间轴

```
按下 ──▶ 气泡出现（0 ms）──▶ 保持 1200 ms ──▶ 淡出 300 ms ──▶ 移除
```

- 出现动画：scale 0.85→1.0 + opacity 0→1，120 ms ease-out。
- 淡出动画：opacity 1→0，300 ms ease-in。
- 若保持期间有新按键，执行"替换+重置计时"（见 4.4）。

### 4.4 连续按键的堆叠与合并

**推荐方案：单气泡替换制（V1.0）。**

- 新按键到达时，当前气泡内容立即替换为新内容，显示计时重置为 1200 ms。
- 优点：屏幕始终只有一个气泡，视觉干净；实现简单、性能开销最小。
- 权衡：无法回看"刚才按了什么"。对教学场景而言，观众注意力在光标附近，单气泡已足够。

**V1.1 可选增强：堆叠模式。**

- 最近 3 条按键以纵向列表显示，最新在底部，旧条目向上淡出。
- 适用于需要展示"操作序列"的高级教学（如 Vim 多步命令）。
- 通过设置项切换，默认关闭。

### 4.5 遮挡与置顶

- 气泡窗口属性：`TopMost` + `ClickThrough`（鼠标事件穿透），不抢焦点。
- 若被全屏独占应用（如游戏）遮挡：检测到全屏独占时自动隐藏气泡并托盘提示，退出全屏后恢复。
- 不尝试绕过 DRM 保护窗口或安全桌面（UAC 提示）。

---

## 5. 功能边界

**结论：本工具是"操作可视化辅助"，不是输入记录器、不是屏幕标注画笔、不是输入法组件。**

| 不做的事 | 理由 |
|----------|------|
| 按键日志/历史记录持久化 | 避免隐私与合规风险（GDPR、企业安全审计）；一旦落盘即具备"键盘记录器"特征，会被杀毒软件拦截，损害产品信任 |
| 屏幕画笔/区域标注 | 那是"屏幕标注工具"（如 ZoomIt、Epic Pen）的职责；混入会模糊产品定位，增加维护面 |
| 输入法候选窗替代/增强 | 输入法事件跨平台差异极大，且涉及用户隐私词库；明确不介入 |
| 宏录制/按键回放 | 属于自动化工具范畴，与"教学可视化"目标无关 |
| 声音反馈（按键音） | V1.0 不做；教学场景通常已有麦克风收音，额外音效反而干扰 |
| 远程/多端同步显示 | 不在单机工具范围内；远程教学由屏幕共享链路解决 |

---

## 6. 可配置项

**结论：提供"基础 5 项 + 高级 7 项"两级设置面板，默认值面向教室投屏优化。**

### 基础设置（主界面可见）

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 启用/暂停 | 开关 | 启用 | 全局热键 `Ctrl+Shift+K` 切换 |
| 显示键盘按键 | 开关 | 开 | — |
| 显示鼠标点击 | 开关 | 开 | — |
| 气泡显示时长 | 滑块 300–5000 ms | 1200 ms | — |
| 气泡不透明度 | 滑块 40%–100% | 85% | — |

### 高级设置（折叠面板）

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 气泡缩放比例 | 滑块 80%–200% | 100% | 适配高分屏/远距离观看 |
| 气泡主题 | 枚举 | 深色 | 深色 / 浅色 / 跟随系统 |
| 字体 | 字体选择器 | 系统等宽 | 仅影响键盘气泡文字 |
| 鼠标涟漪动画 | 开关 | 开 | 关闭后仅显示标签 |
| 排除应用列表 | 列表 | 空 | 指定进程中不显示气泡（如密码管理器） |
| 堆叠模式 | 开关 | 关 | 见 4.4 |
| 开机自启 | 开关 | 关 | 注册为登录项 |

---

## 7. 边界情况

| 场景 | 处理策略 |
|------|----------|
| **多显示器** | 气泡渲染在鼠标当前所在屏幕；各屏幕独立 DPI 感知，不跨屏绘制 |
| **高分屏 / 系统缩放（125%/150%/200%）** | 所有尺寸使用逻辑像素，由系统合成器缩放；气泡窗口标记 DPI-Aware V2，避免模糊 |
| **全屏独占应用（游戏、视频播放器）** | 检测 `DWM` / `NSWindow` 全屏状态 → 自动暂停气泡 → 托盘图标闪烁提示 → 退出全屏后自动恢复 |
| **管理员权限 / 更高完整性进程** | Windows：若目标进程 IL > 当前进程，全局钩子无法捕获按键 → 以"以管理员身份运行"方式启动本工具即可覆盖；安装时默认请求管理员权限（可选降级） |
| **UAC / 安全桌面** | 不注入、不显示；安全桌面切换时自动暂停 |
| **高频连按（>30 次/秒）** | 输入队列合并：100 ms 窗口内的同键重复合并为 `A ×N`；渲染帧率锁定 60 fps，丢弃中间帧 |
| **快速切换焦点窗口** | 气泡不绑定窗口，仅绑定屏幕坐标，焦点切换不影响显示 |
| **系统休眠/锁屏恢复** | 恢复后重新注册全局钩子，重置状态机 |
| **macOS 辅助功能权限未授予** | 首次启动引导用户授权；未授权时仅能捕获鼠标（CGEventTap 限制），键盘事件不可用 → 弹窗提示 |

---

## 8. MVP 范围划分

| 模块 | V1.0 必做 | V1.1+ 迭代 |
|------|-----------|------------|
| 键盘单键 / 组合键显示 | ✅ | — |
| 鼠标三键单击 + 滚轮 | ✅ | — |
| 气泡跟随光标 + 边缘避让 | ✅ | — |
| 单气泡替换 + 淡出 | ✅ | — |
| 全局热键暂停/恢复 | ✅ | — |
| 基础设置（5 项） | ✅ | — |
| 深色/浅色主题 | ✅ | — |
| Windows 10/11 支持 | ✅ | — |
| macOS 12+ 支持 | ✅ | — |
| 双击 / 长按可视化 | — | ✅ |
| 堆叠模式（最近 3 条） | — | ✅ |
| 修饰键预显 | — | ✅ |
| 排除应用列表 | — | ✅ |
| 气泡自定义外观（颜色/圆角/字体） | — | ✅ |
| Linux（X11/Wayland）支持 | — | ✅ |
| 便携版 / 免安装绿色包 | — | ✅ |
| 插件化皮肤系统 | — | ✅ V2.0 |
| 操作序列导出（用于教案） | — | ✅ V2.0 |

---

## 9. 验收标准

以下指标用于判断 V1.0 是否达到"好用"门槛：

| # | 指标 | 目标值 | 测量方法 |
|---|------|--------|----------|
| 1 | 按键→气泡出现延迟 | ≤ 50 ms（P95） | 高速摄像（240 fps）逐帧比对，或程序内打点计时 |
| 2 | CPU 占用（空闲待机） | ≤ 0.5%（单核） | 任务管理器 / Activity Monitor 采样 60 s |
| 3 | 内存占用（稳态） | ≤ 60 MB | 同上 |
| 4 | 连续高频输入（10 次/秒 × 60 s）无丢帧、无崩溃 | 0 次异常 | 自动化压力测试脚本 |
| 5 | 用户主观评价："不需要看说明书就能用起来" | ≥ 4.2 / 5（5 人可用性测试均值） | 任务完成率 + SUS 量表 |

---

## 附录：开放问题（待后续决策）

1. 是否需要提供"教师端控制台"让讲师在投影上远程开关气泡？——取决于产品是否走向多端协同。
2. 气泡是否支持自定义图片/品牌 Logo 水印？——企业内训客户可能有需求，但增加复杂度。
3. 是否将"按键气泡"作为独立产品发布，还是严格绑定"教学工具集合"品牌？——影响命名、分发渠道与定价策略。

---

*文档结束。下一步建议：基于本文档产出交互原型（Figma）与技术选型评估，进入 V1.0 开发排期。*
