- 气泡渲染不走 WebView，由 Rust 原生窗口直接绘制，确保 ≤5 ms 渲染延迟
- 所有输入事件（键盘、鼠标点击、滚轮）共用同一气泡实例与状态机，仅内容映射不同
- 设置面板通过 Tauri IPC 与 Rust 后端通信，修改即时生效无需保存按钮
- 全局钩子回调路径控制在 50 ms 内，高频输入采用 100 ms 合并窗口策略
- 气泡窗口属性固定为 TopMost + ClickThrough，不抢焦点、不拦截下层鼠标事件